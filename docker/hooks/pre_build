#!/bin/bash

#
#  Docker Automated Build Hook
#

apt-get update
apt-get install -y wget

version=$(cat "$DIRSTACK"/version)

wget -O- https://github.com/sigoden/wechat-devtools/releases/download/$version/wechat-devtools-$version.tar.xz | tar xj -C /tmp
mv wechat-devtools-$version ../dist